#!/usr/bin/bash

alias doc=docker-compose
alias docupd='doc up -d'
alias docstop='doc stop'
alias docrmall='doc rm -v -f db matchmaker webapp server run'
alias docrestart='docstop && docrmall && docupd'
